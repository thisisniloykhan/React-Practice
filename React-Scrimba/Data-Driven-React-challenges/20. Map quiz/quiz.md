1. What does the `.map()` array method do?

map method iterates over an array and returns a new array with the same number of items and given condition

2. What do we usually use `.map()` for in React?

we use map method to map a component


3. Critical thinking: why is using `.map()` better than just
   creating the components manually by typing them out?

without using map method we have to put multiple data in each component which takes a lot of time

